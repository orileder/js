const pi = Math.PI
const calculation = (radius) => {
console.log('the area of the circle is: '+ (pi*radius*radius));
console.log('the area of it rounded to two decimal places is: ' + (pi*radius*radius).toFixed(2));
return 
}

calculation(5)